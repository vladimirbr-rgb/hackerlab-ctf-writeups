# Evidence index — Anonymizer

This page documents the public, sanitized evidence for the Anonymizer HackerLab CTF task.

| File | Evidence |
|---|---|
| [`anonymizer-ubuntu-environment-setup.jpg`](./anonymizer-ubuntu-environment-setup.jpg) | Ubuntu VM preparation: `chrony` status check, system-time synchronization, `timedatectl`, package update, and `unzip` installation |
| [`anonymizer-artifact-inventory.jpg`](./anonymizer-artifact-inventory.jpg) | Listing the contents of `anonymizator.zip` and inventorying the available challenge artifacts |
| [`anonymizer-decoder-validation.jpg`](./anonymizer-decoder-validation.jpg) | Syntax validation of the Python decoder `decode.py` with `python3 -m py_compile` |
| [`anonymizer-task-completed.jpg`](./anonymizer-task-completed.jpg) | HackerLab confirmation that the task was solved successfully and the result was accepted; `+400 XP` awarded |

## Publication policy

The public repository intentionally excludes:

- The final CTF flag
- Decoded client records
- Personal data from challenge artifacts
- Private keys, tokens, or credentials
- The original challenge archive and decrypted outputs
